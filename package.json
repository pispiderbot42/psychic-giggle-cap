{
  "name": "psychic-giggle-cap",
  "version": "1.0.0",
  "description": "CAP Hello World on SAP BTP",
  "repository": "https://github.com/pispiderbot42/psychic-giggle-cap.git",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": "^20"
  },
  "dependencies": {
    "@sap/cds": "^7",
    "@sap/xssec": "^3",
    "express": "^4",
    "better-sqlite3": "^9"
  },
  "devDependencies": {
    "@sap/cds-dk": "^7"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cds build --production"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": ":memory:"
        }
      },
      "auth": {
        "kind": "dummy"
      }
    }
  }
}
